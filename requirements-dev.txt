# Testing dependencies
pytest>=7.4.0
pytest-cov>=4.1.0          # coverage report
httpx>=0.25.0               # cho TestClient async

# Core runtime (subset của requirements.txt)
fastapi>=0.104.0
sqlalchemy>=2.0.0
alembic>=1.12.0
pydantic>=2.0.0

# DB drivers
psycopg2-binary>=2.9.9     # PostgreSQL (prod)
python-multipart>=0.0.6     # FastAPI file upload support

# SQLite dùng cho test, built-in Python

# Không cần celery/redis khi chạy unit tests
